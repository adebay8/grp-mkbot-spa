import { DefaultLayout } from "../../layouts";
import { HomeStart } from "../../sections";

const HomePage = () => {
  return (
    <DefaultLayout>
      <HomeStart />
    </DefaultLayout>
  );
};

export default HomePage;
